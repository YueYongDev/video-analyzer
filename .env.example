# 视频分析器环境变量配置文件
# 使用说明：
# 1. 复制此文件为 .env: cp .env.local .env
# 2. 根据需要修改配置值
# 3. 运行测试脚本: python test_service.py <video_file>

# ===== 客户端配置 =====
# 可选值: ollama, openai_api
CLIENT_TYPE=ollama

# ===== Ollama 配置 =====
OLLAMA_URL=http://192.168.100.201:11434
OLLAMA_MODEL=minicpm-v:latest
# 其他可用模型:
# llava: gemma2:9b
# llava:llama3
# bakllava

# ===== OpenAI API 兼容配置 =====
# 当 CLIENT_TYPE=openai_api 时需要设置
OPENAI_API_KEY=your-api-key-here
OPENAI_API_URL=https://openrouter.ai/api/v1
OPENAI_MODEL=meta-llama/llama-3.2-11b-vision-instruct

# ===== 通用配置 =====
# 温度参数，控制生成文本的随机性 (0.0-1.0)
TEMPERATURE=0.2

# 输出目录
OUTPUT_DIR=output

# 是否保留帧文件 (true/false)
KEEP_FRAMES=false

# ===== 音频处理配置 =====
# Whisper 模型大小: tiny, base, small, medium, large
# 或本地模型路径，例如: /path/to/whisper-model
WHISPER_MODEL=medium

# 音频处理设备: cpu, cuda, auto
AUDIO_DEVICE=cpu

# 音频语言代码 (如: en, zh, fr 等)，设为 empty 或注释掉则自动检测
# AUDIO_LANGUAGE=zh

# ===== 帧处理配置 =====
# 每分钟提取的帧数
FRAMES_PER_MINUTE=30

# 最大处理帧数
MAX_FRAMES=100

# ===== 自定义提示 =====
# 自定义分析提示，用于指导视频分析
CUSTOM_PROMPT=

# ===== 网络和缓存配置 =====
# 如果遇到网络问题，可以预先下载模型到本地
# HUGGINGFACE_HUB_CACHE=/path/to/huggingface/cache
# HF_HOME=/path/to/huggingface/home